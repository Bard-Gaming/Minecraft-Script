var is_active = true;

function get_state() {
    print(is_active);
}

function toggle_active() {
    if (is_active) {
        set is_active = false;
    } else (!is_active) {
        set is_active = true;
    }
}