function log_block = () => {
    command("effect give @s minecraft:glowing 1 0 true");
}

function main = () => {
    @a[tag=entity_stare] raycast_entity(log_block, 10);
}

function kill = () => {
    @a[tag=entity_stare] command("tag @s remove entity_stare");
}
