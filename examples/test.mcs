function element_in = (value, list) => {
    for (element in list) {
        log(element);

        if (element == value) {
            return true;
        }
    }

    return false;
}

log("is present:", element_in(11, range(10)));
