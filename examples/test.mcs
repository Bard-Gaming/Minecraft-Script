var bob = "hello";
var bob2 = [bob, "hihi", 3,];

function test = (input) => {
    var bob3 = "bobby car";
    if (input == 3) {
        log("yay");
    } else {
        log(bob2, bob3);
    };
};

test(4);
