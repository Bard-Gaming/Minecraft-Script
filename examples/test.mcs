var coordinates = [1, 2, 3, 4, 5, 6];

function get_block_info = () => {
    for (x in coordinates) {
        for (z in coordinates) {
            log("block: ", get_block(x, 52, z));
            log("");
            log(x, "; ", 52, "; ", z);
        }
    }
}