var is_active = true;

function get_state() {
    log(is_active);
}

function test_fnc() {
    if (is_active) {
        log("Program is active!");
    } else {
        log("Program is inactive.");
    }
}

function toggle_active() {
    set is_active = !is_active;
}