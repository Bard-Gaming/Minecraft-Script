function length = (iterable) => {
    var iterable_len = 0

    for (element in iterable) {
        set iterable_len = iterable_len + 1
    }

    return iterable_len
}

log("range(5) length: ", length(range(5)))
log(length([0, 1, 2]))
log(length([]))